# Schema for the configuration files of the Webform Senha Única module.
webform_senhaunica.settings:
  type: config_object
  label: 'Webform Senha Única settings'
  mapping:
    example:
      type: string
      label: 'Example'

